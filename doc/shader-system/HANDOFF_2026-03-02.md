# ShaderGen-Renderer 解耦交接清单（2026-03-02）

## 1) 当前状态（一句话）
Phase 2 主链路已打通（Contract + Mirror + Adapter + PathMode/Policy + CLI 注入 + 示例入口透传），最后一批示例改动待新机器完成构建回归。

## 2) 已完成范围（可视为稳定基线）
- Shader 契约与镜像导出：ShaderGenContract / ShaderGenResultBuilder 已落地。
- 渲染侧只读适配：RendererShaderGenAdapter 已接入 MaterialManager 双轨对照。
- 路径策略：legacy-only / mirror-validate / mirror-preferred 已可用。
- 配置注入链：GraphicsContext 注入 -> AppFramework 显式配置 -> CLI 参数解析。
- 示例入口：大批示例已改为 os_main(argc, argv) 并透传到 RunFramework 新重载。

## 3) 当前阻塞
- 本机 CMake Tools 异常：构建返回 result code = -1 且无 stdout/stderr。
- 影响：最后一批示例入口迁移后的自动构建验收未完成。

## 4) 新机器 Day-1 执行顺序（照做）
1. 打开工程并确认 CMake Tools 能列出 target。
2. 抽样构建 4 个示例（Environment / Basic / Gizmo / Geometry 各 1 个）。
3. 运行 test_ShaderGenPathMode。
4. 全局检索旧入口形态是否清零。
5. 若回归通过，开始下一阶段功能推进。

## 5) 建议抽样目标
- 03_BasicLitSunDirectionECS
- 01_AtmosphereSkyMinimal
- 05_GizmoUsageExample
- 01_ExtrudedPolygonTest

## 6) 覆盖确认检索项
- 检索 1：int os_main(int, os_char**)（预期 0）
- 检索 2：示例中 RunFramework 老调用（未透传 argc/argv）是否残留

## 7) 回归通过后的下一步
1. 补齐剩余示例入口透传（若仍有漏网）。
2. 增强 DiffKV 日志聚合（按 material/stage 过滤）。
3. 增加 mirror-preferred 严格模式端到端失败路径回归。

## 8) 风险评估
- 风险等级：中低。
- 原因：代码级静态错误已清零，主要风险集中在“最后一批改动未完成构建验收”。

## 9) 关联文档
- 总计划与详细进展：doc/shader-system/SHADERGEN_RENDERER_DECOUPLING_PLAN.md
