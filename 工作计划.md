# 六、Shader 生成器专项工作计划（新增，2026-02）

### 6.1 顶层约束（强约束）

1. 渲染目标以“一个 Drawcall 全场景”为硬约束，Shader 生成器不得引入破坏批处理一致性的自由度。
2. 3D 材质走“现代固定管线”路线，不走 Unreal 式超自由化。
3. 开发者输入限定为：
  - 光照参数
  - 数据需求（required_resources）
  - 光照/着色函数入口（受限签名）
4. 生成器自动合成以下标准模式变体：
  - 渲染路径：Forward / GBuffer Deferred / VBuffer Deferred / MobileSubpassGBufferDeferred / PostProcess
  - 覆盖模式：Solid / Mask / DepthOnlySolid / DepthOnlyMask
  - 输入模式：VertexInput / SSBO VertexInput（长期目标：全 SSBO）
  - 阶段拓扑：VS/FS / MeshShader/FS
  - GBuffer格式级别：MobileLite / MobileExtended / DesktopStandard / DesktopFull / Custom
  - 后处理输出：GBuffer 通道组合掩码（必须为当前 GBuffer 格式子集）
  - MotionVector：可选通道（移动端默认关闭，按需启用）
  - 前向光照：PerPixel / PerVertex（低配与远景）
  - 法线压缩：VertexInput / NormalMap / GBuffer 三路可独立启停，开发者默认使用解压后法线
5. Shader 生成器目标可独立构建为程序或动态库（dll/so），不依赖引擎基础库类型。
6. 运行时数据链路固定为“Instanced ID 间接寻址”模型：
  - Instanced Vertex Attribute 传 `L2W_ID` 与 `MI_ID`
  - `L2W_ID -> TransformSSBO` 获取 LocalToWorld 等对象变换数据
  - `MI_ID -> MaterialInstanceSSBO` 获取参数与纹理 Layer ID
  - 纹理采样统一通过 TextureArray/CubemapArray + Layer ID 完成
  - 单 Drawcall 过程中不做对象级 descriptor 切换

### 6.2 架构重设计方向

#### Phase SG-1（M1）：模式矩阵与接口收敛
- [ ] 定义 `PipelineMode` 六轴模型（Path/Coverage/Input/Topology/GBufferFormat/ForwardLighting）
- [ ] 定义 `PostProcessOutputMask`（GBuffer 通道组合掩码）
- [ ] 建立模式组合白名单和非法组合校验
- [ ] 将现有 Composed 入口改为 `MaterialSpec + PipelineMode` 驱动

#### Phase SG-2（M2）：固定流程 Composer 分层
- [ ] 输入层：VertexInput 与 SSBO VertexInput 双实现（主路径 SSBO）
- [ ] 阶段层：VS/FS 与 MeshShader/FS 双实现（主路径 Mesh/FS）
- [ ] 输出层：Forward / GBuffer / VBuffer / MobileSubpassGBuffer / PostProcess 五实现
- [ ] 覆盖层：Solid / Mask / DepthOnlySolid / DepthOnlyMask（含 Alpha Cutoff 与纯深度规则）
- [ ] GBuffer 输出层：按格式级别自动生成参数结构与输出布局
- [ ] GBuffer 格式支持 `enable_motion_vector` 开关（默认不强制写入 MotionVector）
- [ ] 法线压缩策略层：支持 VertexInput/NormalMap/GBuffer 编码策略，自动插入 encode/decode helper
- [ ] 后处理输出层：支持任意 GBuffer 通道组合掩码（非单通道）
- [ ] 光照层：参数块 + 函数入口（禁止任意拼接）
- [ ] 前向光照分层：PerPixel / PerVertex 双实现（PerVertex 面向低配与远景）
- [ ] Mobile Subpass GBuffer 路径：支持 Vulkan `subpassLoad` 输入附件读取

#### Phase SG-3（M3）：独立库化
- [ ] 拆分 `ShaderGenCore`（无引擎基础库依赖）
- [ ] 核心容器改为 STL（按需接入 Abseil/ankerl）
- [ ] 输出 C API 或稳定 C++ API，支持 dll/so 嵌入

#### Phase SG-4（M4）：产物与验证
- [ ] 生成模式覆盖率报告（Forward/GBuffer/VBuffer × Solid/Mask × Input）
- [ ] 生成缺失资源与不兼容组合诊断报告
- [ ] 建立“单 Drawcall 兼容性”回归检查项

### 6.3 阶段验收标准（新增）

#### SG-1 验收
- [ ] `PipelineMode` 六轴可完整表达：Path/Coverage/Input/Topology/GBufferFormat/ForwardLighting
- [ ] `PostProcessOutputMask` 支持组合表达，且具备格式子集校验
- [ ] 非法组合可在生成阶段被拦截，并返回可读错误
- [ ] 旧入口兼容：默认映射到 `VS/FS + VertexInput`（不破坏现有测试）

#### SG-2 验收
- [ ] 输入层双路径（VertexInput/SSBO）在同材质下输出语义一致
- [ ] 阶段层双路径（VS/FS 与 Mesh/FS）在同材质下输出语义一致
- [ ] Forward/GBuffer/VBuffer 输出字段定义稳定，不因路径切换而改变语义
- [ ] DepthOnlySolid/DepthOnlyMask 不产生颜色输出，且可用于 ShadowMap/Early-Z
- [ ] 光照算法档位与 GBuffer 格式可联动配置，并可自动生成 shader 参数结构
- [ ] PostProcess 通道组合输出可独立路由，且严格受 GBuffer 格式子集约束
- [ ] Forward `PerVertex` 光照可用于低配/远景，且与 `PerPixel` 在可接受误差内一致
- [ ] MobileSubpassGBuffer 路径可通过 `subpassLoad` 读取前序 subpass 数据，且与常规 GBuffer 语义对齐
- [ ] VertexInput/NormalMap/GBuffer 法线可配置压缩，业务侧默认拿到解压后法线参与光照计算

#### SG-3 验收
- [ ] `ShaderGenCore` 能独立编译（不依赖引擎基础容器）
- [ ] 提供最小 API 用例（加载配置 → 生成 GLSL/SPV → 导出诊断）
- [ ] 动态库与工具程序两种形态至少一种稳定可运行

#### SG-4 验收
- [ ] 覆盖率报告可枚举并统计全部目标组合
- [ ] 诊断报告可定位到具体模式轴与资源项
- [ ] 单 Drawcall 兼容性回归通过（对象级差异仅通过 ID 间接寻址）
- [ ] PostProcess 路径可报告通道组合覆盖率与“非格式子集”冲突配置

### 6.4 输入/拓扑自适应策略（新增）

- [ ] 输入策略：`PreferSSBO` / `ForceVertexInput` / `AutoByCapability`
- [ ] 拓扑策略：`PreferMeshShader` / `ForceVSFS` / `AutoByCapability`
- [ ] 回退顺序（默认）：`Mesh/FS + SSBO` → `VS/FS + SSBO` → `VS/FS + VertexInput`
- [ ] 回退后保持同一 MaterialSpec 语义，不允许出现参数解释差异

### 6.5 本周落地项（可执行）
- [ ] 在 `ComposedShaderGenerator` 引入 `PipelineMode` 占位结构与路由函数（含 Topology 轴）
- [ ] 新增 `GBuffer` / `VBuffer` 输出合成占位实现（先不引入复杂材质图）
- [ ] 新增 `MobileSubpassGBuffer` 路径占位与 `subpassLoad` 宏路由
- [ ] 新增 `GBufferFormat` 默认级别与通道掩码映射
- [ ] 新增 `PostProcess` 路径占位与通道组合掩码路由（受 GBuffer 格式约束）
- [ ] 新增 Forward `PerVertex` 光照占位路由（先打通路径，再做实际光照公式）
- [ ] 为 `required_resources` 增加模式相关校验（例如某模式必需 camera/light 输入）
- [ ] 增加 `VS/FS` 与 `Mesh/FS` 自适应选择策略（按能力自动降级）
- [ ] 增加独立构建可行性检查（最小依赖清单：STL + Vulkan headers）
