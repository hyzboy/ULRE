# ULRE Buffer 系统工作计划

**创建日期**: 2026-02-22  
**范围**: `inc/hgl/vk/` + `src/Vulkan/` + `src/ecs/` 中的全部缓冲区管理代码

---

## 一、当前架构全景

### 1.1 层次关系（现状）

```
┌─────────────────────────────────────────────────────────┐
│  Layer 3 — 数据访问层                                     │
│  BufferAccessor<T>   StructuredBufferAccessor<T>         │
│  VKBufferMap<T>/VABMap   (后者已无 .cpp 使用)             │
│  RingBufferWrapper : BufferWriteAgent                    │
└────────────────────┬────────────────────────────────────┘
                     │ 持有 DeviceBuffer*
┌────────────────────▼────────────────────────────────────┐
│  Layer 1 — GPU Buffer 持有层                              │
│  DeviceBuffer / VAB (VertexAttribBuffer) / IndexBuffer   │
│      成员: StagedBuffer *staged_source (可选)            │
│      Map/Write/Flush 全部转发给 staged_source            │
└────────────────────┬────────────────────────────────────┘
                     │ owns (DeviceBuffer 析构时 delete)
┌────────────────────▼────────────────────────────────────┐
│  Layer 2 — GPU 上传层 (IGPUBuffer 接口)                   │
│  StagedBuffer : IGPUBuffer                               │
│      staging buffer (CPU) + device buffer (GPU)         │
│      构造时注册到 VulkanDevice::gpu_buffer_registry      │
└────────────────────┬────────────────────────────────────┘
                     │ ECS 轮询
┌────────────────────▼────────────────────────────────────┐
│  ECS — 上传调度层                                         │
│  RenderBufferUploadSystem                                │
│      遍历 gpu_buffer_registry，对 IsDirty() 的 buffer    │
│      调用 CopyToDevice(vk_cmd)                           │
└─────────────────────────────────────────────────────────┘

※ 另有独立路径（不经过 IGPUBuffer registry）：
   RingBufferWrapper : BufferWriteAgent
      → DeviceBufferRingWriter → DeviceBuffer（CPUVisible/ReBAR）
      → 由 TransformAssignmentBuffer / MaterialInstanceAssignmentBuffer 直接驱动
```

### 1.2 已完成的修复（本阶段）

| 序号 | 问题 | 修复方式 | 状态 |
|------|------|----------|------|
| F-1 | `RenderBufferUploadSystem` 只在 `InstallPrimitiveGroup` 中注册，Billboard 等简单应用不经过该路径 | 改为在 `ECSContext::Initialize()` 中无条件注册，与 `TransformSystem` 同级 | ✅ 完成 |
| F-2 | `StagedBuffer::Unmap()` 不标记 dirty，通过 Map 指针写入的数据永远不上传 | `Unmap()` 内部调用 `MarkDirty(0, VK_WHOLE_SIZE)` | ✅ 完成 |
| F-3 | `IGPUBuffer` 的名字在创建后仍可被 `SetDebugName` 修改 | 改为 `buffer_name`（const），构造时传入，删除 setter | ✅ 完成 |
| F-4 | `CreateIBO` 的 `ObjectNameBuilder` 名字硬编码为 `"IBO"` | 增加接受 `ObjectNameBuilder name` 的重载，调用方传入几何体名称 | ✅ 完成 |

---

## 二、已识别问题清单

### 【BUG 类】

#### BUG-1: `Unmap` 的 dirty range 过于保守
- **文件**: `src/Vulkan/VKStagedBuffer.cpp`
- **现状**: `Map(offset, size)` 知道映射范围，但 `StagedBuffer` 未记录这两个值，所以 `Unmap()` 只能将整个 buffer 标脏。即使只 Map 了 16 字节也会 transfer 整个 4MB buffer。
- **期望**: `Unmap()` 只脏实际被 Map 的范围。
- **改法**:
  1. `StagedBuffer` 增加私有成员 `VkDeviceSize mapped_offset = 0; VkDeviceSize mapped_size = 0;`
  2. `Map(offset, size)` 时记录这两个值（注意字节单位，VAB/IBO 的 `Map` 在调用 `DeviceBuffer::Map` 时会乘以 stride，所以在 `StagedBuffer::Map` 层已经是字节偏移）
  3. `Unmap()` 改为 `MarkDirty(mapped_offset, mapped_size)`，然后清零记录值

---

### 【冗余/死代码类】

#### DEAD-1: `BufferAccessBase::dirty` 字段从不驱动 GPU 上传
- **文件**: `inc/hgl/vk/VKBufferAccessBase.h`
- **现状**: `BufferAccessBase` 有一套独立的 `dirty/MarkDirty()/IsDirty()/ClearDirty()` 方法，但 `RenderBufferUploadSystem` 完全不读这个字段，GPU 上传由 `StagedBuffer::is_dirty` 驱动。两套系统完全没有桥接，`accessor.MarkDirty()` 不会触发任何上传。
- **风险**: 误导开发者认为调用 `MarkDirty()` 后数据会上传 GPU。
- **改法（两个方向选一）**:
  - **方向 A（推荐）**: 删除 `BufferAccessBase` 中的 dirty 字段及相关方法，注释说明 GPU dirty 由 `Write()/Unmap()` 路径自动驱动，无需 accessor 层追踪。
  - **方向 B**: 重新赋予其明确语义，改名为 `cpu_modified`，用于上层逻辑的"我写过这个 accessor"判断（例如跳过没有变化的 transform），但要在注释中明确写明与 GPU 上传无关。

#### DEAD-2: `VKBufferMap<T>` / `VABMap` 已无实际使用
- **文件**: `inc/hgl/vk/VKBufferMap.h`, `inc/hgl/vk/VKVertexAttribBuffer.h`
- **现状**: 
  - `VABMap` 类定义在 `VKVertexAttribBuffer.h` 中，`GeometryCreater.h` 包含了 `VKBufferMap.h`，但所有 `.cpp` 中均无 `VABMap` 的实例化使用
  - `BufferAccessor<T>` 已经完全覆盖 `VABMap` 的全部功能，且额外提供了 RAII 保护和 dirty 追踪
  - `VKBufferMap::Write(data, c)` 在已 Map 情况下是裸 `memcpy`，未走 `Write()` 路径（当前靠 `Unmap` 补 dirty，逻辑上脆弱）
- **改法**:
  1. 在 `VABMap` 和 `VKBufferMap` 声明处加 `[[deprecated]]` + 注释"请使用 BufferAccessor<T>"
  2. 从 `GeometryCreater.h` 中移除 `#include<hgl/vk/VKBufferMap.h>`（该头文件已通过 `VKVertexAttribBuffer.h` 间接包含）
  3. 待观察期（1~2个月）后若无新增使用，安排删除

#### DEAD-3: `VKBufferWriteAgent` 是单一实现的无用抽象
- **文件**: `inc/hgl/vk/VKBufferWriteAgent.h`, `inc/hgl/vk/VKRingBufferWrapper.h`
- **现状**:
  - `BufferWriteAgent` 定义了 `MapRange/Unmap/WriteRange/HasPendingUpload/MarkDirty/IsDirty/CommitInternal/GetBuffer` 纯虚接口
  - 唯一实现者是 `RingBufferWrapper`
  - 使用方（`TransformAssignmentBuffer`、`MaterialInstanceAssignmentBuffer`）持有的是 `RingBufferWrapper` 具体类，完全不通过 `BufferWriteAgent` 接口调度
  - `CommitInternal()` 的实现仅 `ClearDirty()`（Ring buffer 不需要 staging，数据已在 CPU-visible buffer），没有人调用它
- **改法**:
  - 短期：在 `VKBufferWriteAgent.h` 头部加注释说明现状
  - 中期：如果第二个实现者（如 `StagedBufferWriteAgent`）始终不出现，删除 `BufferWriteAgent` 抽象，把 `RingBufferWrapper` 改为普通类

---

### 【设计缺陷类】

#### DESIGN-1: Ring buffer 与 Staged buffer 是两套完全断开的提交路径
- **现状**:

  | | StagedBuffer 路径 | RingBuffer 路径 |
  |---|---|---|
  | 注册 | `VulkanDevice::gpu_buffer_registry` | 不注册 |
  | 上传触发 | `RenderBufferUploadSystem` 轮询 `is_dirty` | 外部直接调用 `WriteDynamicRange()` |
  | GPU 写入时机 | 下一帧渲染命令开始前（barrier 保证） | 立即（CPU-visible buffer，无需 copy） |
  | 使用场景 | VBO/IBO/UBO 静态或低频更新 | Transform/MaterialInstance 每帧动态更新 |

  两路并存本身合理（不同 GPU 内存类型的最优策略不同），但文档和注释完全没有说明这个二元性，导致维护者困惑。

- **改法**: 在 `IGPUBuffer.h` 和 `VKRingBufferWrapper.h` 头部各加一段说明，解释两套路径的适用场景和边界。无需改代码。

#### DESIGN-2: 层级倒置——`DeviceBuffer` owns `StagedBuffer` 但 `StagedBuffer` 是更高层的概念
- **已完成（Phase 3c）**: 提取 `VkBufferOwner` 基类，结构倒置问题基本消除。
- **当前架构（Phase 3c 完成后）**:
  ```
  VkBufferOwner (持有 VkDevice, DeviceBufferData, IGPUBuffer*, BufferUpdateClass)
      ├── DeviceBuffer（保留 Map/Write/Flush 过渡转发器，待最终删除）
      ├── VAB   (直接继承 VkBufferOwner，不再经过 DeviceBuffer)
      ├── IBO   (直接继承 VkBufferOwner)
      └── ICB   (直接继承 VkBufferOwner)

  IGPUBuffer (接口：Write/Map/Unmap/IsDirty/CopyToDevice)
      ├── StagedBuffer  (注册 registry)
      ├── ReBarBuffer   (注册 registry，CopyToDevice no-op)
      └── RingBuffer    (不注册，外部驱动)
  ```
- **剩余工作**: 删除 `DeviceBuffer::Map/Write/Flush/Unmap` 过渡转发器（阻塞于 `VKMemoryAllocator` 豁免路径）。

#### DESIGN-3: `RingBufferWrapper::CommitInternal()` 语义空洞
- **现状**: 实现只有 `ClearDirty(); return true;` —— Ring buffer 写入即可见，不需要任何操作。但 `BufferWriteAgent` 抽象的设计意图是"提交到 GPU"，这与 ring buffer 的 no-op commit 本质矛盾。
- **改法**: 要么删除 `BufferWriteAgent`（见 DEAD-3），要么拆分接口为 `HasPendingUpload()` 检查型和 `CommitIfNeeded()` 执行型，并在文档中说明 ring buffer 返回 false/no-op。

---

## 三、工作计划

### Phase 1 — 近期（低风险，单文件）✅ 已完成

**目标**: 消除精确度问题，补充关键注释。  
**完成日期**: 2026-02-22

| 任务 | 文件 | 优先级 | 依赖 | 状态 |
|------|------|--------|------|------|
| BUG-1：精确 dirty range | `VKStagedBuffer.h/.cpp` | 高 | 无 | ✅ 完成 |
| DEAD-1：删除 `BufferAccessBase::dirty`，在 `BufferAccessor<T>` 和 `StructuredBufferAccessor<T>` 中各自保留 private `dirty` 字段 | `VKBufferAccessBase.h`, `VKBufferAccessor.h`, `StructuredBufferAccessor.h` | 中 | 无 | ✅ 完成 |
| DESIGN-1：补充两路径说明注释 | `IGPUBuffer.h`, `VKRingBufferWrapper.h` | 低 | 无 | ✅ 完成 |

---

### Phase 2 — 中期（多文件，需测试）✅ 已完成

**完成日期**: 2026-02-22

| 任务 | 文件 | 状态 |
|------|------|------|
| DEAD-2：`VABMap`/`VKBufferMap` 加注释 + `[[deprecated]]` | `VKBufferMap.h`, `VKVertexAttribBuffer.h`, `GeometryCreater.h` | ✅ 完成 |
| DEAD-3：`BufferWriteAgent` 状态说明，判断为 dead abstraction | `VKBufferWriteAgent.h` | ✅ 完成 |
| DESIGN-3：`RingBuffer` 提交路径文档化 | `VKRingBufferWrapper.h` | ✅ Phase 1 已补充 |

---

### Phase 3 — 长期（架构变更，渐进迁移）

**目标**: 修正层级倒置，使架构自洽。

#### Phase 3a — 暴露迁移接口（纯加法）✅ 已完成 2026-02-22

| 任务 | 文件 | 状态 |
|------|------|------|
| `DeviceBuffer::GetGPUBuffer()` — 返回 `IGPUBuffer*`，暴露上传路径接口 | `VKBuffer.h/.cpp` | ✅ 完成 |
| `BufferAccessBase::GetGPUBuffer()` — 透传 | `VKBufferAccessBase.h` | ✅ 完成 |
| `BufferAccessBase::Write()` — 优先走 `IGPUBuffer*` 路径（语义不变）| `VKBufferAccessBase.h` | ✅ 完成 |
| `DeviceBuffer::Write/Map/Flush` 加过渡注释 | `VKBuffer.h` | ✅ 完成 |

#### Phase 3b — 逐步迁移调用方 ✅ 已完成 2026-02-22

新增实现：**`ReBarBuffer : public IGPUBuffer`**（`VKReBarBuffer.h/.cpp`）
- CPUVisible/ReBAR 路径缺少 `IGPUBuffer` 实现，`GetGPUBuffer()` 对 CPUVisible buffer 返回 null，导致运行时崩溃
- 创建 `ReBarBuffer`，`Map/Write/Unmap` 直接操作 CPU-visible `DeviceMemory`，`CopyToDevice` 为 no-op
- `DeviceBuffer::staged_source` 类型从 `StagedBuffer*` 改为 `IGPUBuffer*`（同时兼容 StagedBuffer 和 ReBarBuffer）
- 所有工厂函数（VAB/IBO/Buffer/ICB）的 CPUVisible 路径均创建 `ReBarBuffer` 并 `SetStagedSource`
- **`GetGPUBuffer()` 对有 CPU 写路径的 buffer 永远非 null**

| 任务 | 文件 | 状态 |
|------|------|------|
| `StructuredBufferAccessor::MapInternal/UnmapInternal/CommitInternal/ImmediateUpdate/Write` 改用 `GetGPUBuffer()` | `StructuredBufferAccessor.h` | ✅ 完成 |
| `TransformAssignmentBuffer` 所有 Map/Unmap 写入点改用 `GetGPUBuffer()` | `TransformAssignmentBuffer.cpp` | ✅ 完成 |
| `MaterialInstanceAssignmentBuffer` 改用 `GetGPUBuffer()` | `MaterialInstanceAssignmentBuffer.cpp` | ✅ 完成 |
| `TextRenderPipeline` Write 调用改用 `GetGPUBuffer()` | `TextRenderPipeline.cpp` | ✅ 完成 |
| `VAB::Map/Flush/Write/Unmap` 覆写直接调用 `GetGPUBuffer()`，绕过 `DeviceBuffer` 转发层 | `VKVertexAttribBuffer.h` | ✅ 完成 |
| `IndexBuffer::Map/Flush/Write/Unmap` 覆写直接调用 `GetGPUBuffer()` | `VKIndexBuffer.h` | ✅ 完成 |
| `IndirectCommandBuffer::Map/Flush/Write/Unmap/WriteCmd` 覆写直接调用 `GetGPUBuffer()` | `VKIndirectCommandBuffer.h` | ✅ 完成 |
| `DeviceBuffer::Unmap()` 改为 virtual | `VKBuffer.h` | ✅ 完成（子类覆写需要） |

**剩余 `DeviceBuffer` 过渡转发器调用方（暂不迁移）**：
- `TileData.cpp`：`tile_buffer->Map()/Unmap()` — CPUOnly 纹理暂存缓冲，没有 `IGPUBuffer*`（不走 ECS 注册），不迁移
- `VKMemoryAllocator.cpp`：豁免（旧式 VulkanArrayBuffer 路径，见注意事项 1）

#### Phase 3c — 结构重组（破坏性，需全量测试）

**前置条件**：Phase 3b 完成 ✅

**已完成步骤：**

**Phase 3c Step 1 ✅ — 过渡转发器废弃标记（2025完成）**

关键发现与决策：
- MSVC 中对 `virtual` 函数加 `[[deprecated]]` 会**改变 mangled symbol 名称**（`QEAA→UEAA`），导致
  预编译 `.obj` 文件链接失败（LNK2001）。因此**虚函数版本不加 `[[deprecated]]`**，改用 `// TODO` 注释。
- 项目已全局禁用 C4996（`<DisableSpecificWarnings>...4996</DisableSpecificWarnings>`），
  `[[deprecated]]` 不会产生编译警告，仅作为**源码文档及 IDE tooltip 提示**使用。
- 非虚 `Map()` / `Unmap()` / `Write(const void*)` 已加 `[[deprecated]]` 标记。

```cpp
// VKBuffer.h 现状：
[[deprecated("Use GetGPUBuffer()->Map(0,GetSize()) instead.")]]
        void *  Map     ();                                    // 非虚，已标记
virtual void *  Map     (VkDeviceSize start,VkDeviceSize size);  // 虚，TODO 注释
[[deprecated("Use GetGPUBuffer()->Unmap() instead.")]]
        void    Unmap   ();                                    // 非虚，已标记
virtual void    Flush   (VkDeviceSize start,VkDeviceSize size);  // 虚，TODO 注释
virtual void    Flush   (VkDeviceSize size);                     // 虚，TODO 注释
virtual bool    Write   (const void *ptr,uint32_t start,uint32_t size); // 虚，TODO 注释
virtual bool    Write   (const void *ptr,uint32_t size);               // 虚，TODO 注释
[[deprecated("Use GetGPUBuffer()->Write(ptr,0,GetSize()) instead.")]]
        bool    Write   (const void *ptr);                     // 非虚，已标记
```

**已完成步骤续（Phase 3c 后期，2026-02-23）：**

- 提取 `VkBufferOwner` 基类（`VKBufferOwner.h/.cpp`），将 `DeviceBuffer` 的 VkBuffer 容器职责下沉；`DeviceBuffer` 改为继承 `VkBufferOwner`；`VAB/IBO/ICB` 直接继承 `VkBufferOwner`，彻底脱离 `DeviceBuffer`。
- `BufferAccessBase::buffer` 字段类型从 `DeviceBuffer*` 升级为 `VkBufferOwner*`；`SetBuffer()`/`TrackBuffer()` 等全链路更新。
- 全部 `[[deprecated]] BindUBO/BindSSBO(DeviceBuffer*)` 重载从 `VKDescriptorSet`/`VKMaterialParameters`/`VKMaterial` 的声明和实现中彻底删除；`DescriptorBufferInfo` 构造参数从 `DeviceBuffer*` 升级为 `VkBufferOwner*`。
- `StructuredBufferAccessor<T>` 所有构造器、`Create()`、`Bind()` 参数从 `DeviceBuffer*` 升级为 `VkBufferOwner*`；移除了对 `VKBuffer.h` 的依赖。

**待执行步骤：**

| 任务 | 文件 | 说明 | 状态 |
|------|------|------|------|
| 过渡转发器标记（非虚加 `[[deprecated]]`，虚加 TODO）| `VKBuffer.h` | 已完成，C4996 全局禁用故仅为文档 | ✅ |
| Accessor 写路径绕过 DeviceBuffer 转发器：`MapInternal/UnmapInternal/WriteBulk/Flush` 均改走 `GetGPUBuffer()` | `VKBufferAccessBase.h`, `VKBufferAccessor.h` | `buffer` 字段仍为 `DeviceBuffer*`（GetBuffer()/GetBufferInfo()/static_cast<VAB*> 需保留）；写路径已完全脱离 transitionnal forwarder | ✅ |
| `BufferAccessBase` 中缓存 `IGPUBuffer *gpu_buf`，`SetBuffer()` 同步赋值；`GetGPUBuffer()` 直接返回缓存值，不再经由 `buffer->GetGPUBuffer()` 链 | `VKBufferAccessBase.h`, `VKBufferAccessBase.cpp` | `Write()`/`Flush()` 均直接使用 `gpu_buf`，不再需要 null-check `buffer` | ✅ |
| `BufferAccessor<T>` 和 `StructuredBufferAccessor<T>` 内所有 `buffer` 守卫路径（MapInternal / UnmapInternal / WriteBulk / CommitInternal / IsValid 等）全改为 `gpu_buf` | `VKBufferAccessor.h`, `StructuredBufferAccessor.h` | 完全搞除内部对 `buffer` 字段的写路径依赖 | ✅ |
| `VAB / IBO / IndirectCommandBuffer<T>` 加 `GetVkBuffer()` （返回 `GetGPUBuffer()->GetVkDeviceBuffer()`）；ICB `Draw/DrawIndexed/Dispatch` 改用 `GetVkBuffer()` 替换直接访问 `buf.buffer`；`Primitive.cpp` / `VKGeometry.cpp` / `VKCommandBufferRender.cpp` 的 VkBuffer 提取改用 `GetVkBuffer()` | `VKVertexAttribBuffer.h`, `VKIndexBuffer.h`, `VKIndirectCommandBuffer.h`, `Primitive.cpp`, `VKGeometry.cpp`, `VKCommandBufferRender.cpp` | 热路径不再依赖 `DeviceBuffer::GetBuffer()`/`buf.buffer`；VAB/IBO/ICB 仳继承 DeviceBuffer 但语义上将可脱离 | ✅ |
| `VAB/IBO/ICB` 脱离 `DeviceBuffer` 继承；提取 `VkBufferOwner` 基类 | `VKBufferOwner.h/.cpp`, `VKVertexAttribBuffer.h`, `VKIndexBuffer.h`, `VKIndirectCommandBuffer.h`, `VKBuffer.h`, `VKBufferAccessBase.h/.cpp`, `VKDevice.h/.cpp`, `src/SceneGraph/module/BufferManager.cpp` | 建立 `VkBufferOwner`（持有 VkDevice/DeviceBufferData/IGPUBuffer*/BufferUpdateClass）；`DeviceBuffer:VkBufferOwner`；`VAB/IBO/ICB:VkBufferOwner`；全链路 SetBuffer/TrackBuffer 升级为 `VkBufferOwner*`；backward-compat shims 全部删除 | ✅ 2026-02-23 |
| `BindUBO/BindSSBO(DeviceBuffer*)` 全链路彻底删除（非降级，直接移除） | `VKMaterial.h/.cpp`, `VKMaterialParameters.h/.cpp`, `VKDescriptorSet.h/.cpp` | 所有 `DeviceBuffer*` 重载从头文件声明和 cpp 实现中完全删除；offset+range 变体参数类型升级为 `VkBufferOwner*`；`DescriptorBufferInfo` 构造参数升级为 `VkBufferOwner*` | ✅ 2026-02-23 |
| `DescriptorSet::BindUBO/BindSSBO` 新增 `IGPUBuffer*` 重载；`MaterialParameters`/`Material` 同步新增 `IGPUBuffer*` 重载；`DescriptorBinding` maps 改为 `const IGPUBuffer*` + `AddUBO/AddSSBO` 改走 `GetGPUBuffer()`；`BufferAccessBase::ubo()` 返回 `IGPUBuffer*` | `VKDescriptorSet.h/.cpp`, `VKMaterialParameters.h/.cpp`, `VKMaterial.h/.cpp`, `VKDescriptorBindingManage.h/.cpp`, `VKBufferAccessBase.h` | 描述符绑定链全链路 IGPUBuffer* 化，LineRenderManager BindUBO 迁移至 IGPUBuffer* 重载 | ✅ 2026-02-23 |
| 处理 `ChangeTexture2D`：CopyBufferToImage/CommitTexture 私有方法改为 `VkBuffer` 参数；公开 `DeviceBuffer*` API 在入口处提取 `->GetBuffer()` | `TextureManager.h`, `VKDeviceTexture.cpp`, `VKDeviceTexture2D.cpp`, `VKDeviceTextureCube.cpp`, `VKDeviceTexture2DArray.cpp` | 纹理上传内部路径不再传递 DeviceBuffer* | ✅ 2026-02-23 |
| `DeviceBuffer` 退化为纯 VkBuffer 容器，删除 `Map/Write/Flush/Unmap` | `VKBuffer.h/.cpp` 及 VKMemoryAllocator | 依赖 VAB/IBO/ICB 脱继承全部完成 | ⏳ |

---

## 四、文件职责速查

| 文件 | 当前职责 | 状态 |
|------|----------|------|
| `IGPUBuffer.h` | Layer2 统一接口，ECS 上传系统的合约 | ✅ 清晰 |
| `VKStagedBuffer.h/.cpp` | IGPUBuffer 实现（staged：CPU staging + GPU device），精确 dirty range | ✅ 完成 |
| `VKReBarBuffer.h/.cpp` | IGPUBuffer 实现（ReBAR/CPUVisible：单块 CPU-visible+Device-local 内存，CopyToDevice no-op） | ✅ Phase 3b 新增 |
| `VKBufferOwner.h/.cpp` | VkBuffer 纯容器基类（`VkDevice device`, `DeviceBufferData buf`, `IGPUBuffer* staged_source`, `BufferUpdateClass update_class`）；虚析构负责清理；`GetBuffer/GetMemory/GetGPUBuffer/GetSize/SetUpdateClass/GetBufferInfo` 等公共访问器 | ✅ Phase 3c 新增 2026-02-23 |
| `VKBuffer.h/.cpp` | `DeviceBuffer : public VkBufferOwner`；保留 `Map/Write/Flush/Unmap` 过渡转发器（阻塞于 VKMemoryAllocator 豁免路径；非虚标 `[[deprecated]]`，虚留 TODO 注释） | ⚠️ 待 VKMemoryAllocator 迁移后删除转发器 |
| `VKVertexAttribBuffer.h` | `VAB : public VkBufferOwner`（已脱离 DeviceBuffer）；Map/Write/Flush/Unmap 直接调用 `GetGPUBuffer()`；`GetVkBuffer()` 返回 `GetGPUBuffer()->GetVkDeviceBuffer()`；VABMap `[[deprecated]]` | ✅ Phase 3c 完成 2026-02-23 |
| `VKIndexBuffer.h` | `IBO : public VkBufferOwner`（已脱离 DeviceBuffer）；Map/Write/Flush/Unmap 直接调用 `GetGPUBuffer()`；`GetVkBuffer()` | ✅ Phase 3c 完成 2026-02-23 |
| `VKIndirectCommandBuffer.h` | `ICB<T> : public VkBufferOwner`（已脱离 DeviceBuffer）；Map/Write/Flush/Unmap/WriteCmd 直接调用 `GetGPUBuffer()`；`GetVkBuffer()`；Draw/DrawIndexed/Dispatch 用 `GetVkBuffer()` | ✅ Phase 3c 完成 2026-02-23 |
| `VKBufferMap.h` | VABMap 基类模板，已加 `DEPRECATED` 注释 | ✅ DEAD-2 已处理 |
| `VKBufferAccessBase.h` | Accessor 基类；`buffer` 字段类型为 `VkBufferOwner*`；`IGPUBuffer *gpu_buf` 缓存字段；`SetBuffer(VkBufferOwner*)` 同步赋值；`GetGPUBuffer()` 直接返回 `gpu_buf`；`Write()`/`Flush()` 直接通过 `gpu_buf` 操作；`ubo()` 返回 `VkBufferOwner*` | ✅ Phase 3c 全部完成 |
| `VKBufferAccessor.h` | BufferAccessor<T> RAII 访问器；MapInternal/UnmapInternal/WriteBulk 直接调用 `GetGPUBuffer()`；`GetBuffer()` 用 `typed_vab` 字段（无 UB static_cast） | ✅ 写路径+GetBuffer() Phase 3c 完成 |
| `StructuredBufferAccessor.h` | 结构体映射访问器；所有构造器、`Create()`、`Bind()` 参数为 `VkBufferOwner*`；全路径走 `GetGPUBuffer()`；不再包含 `VKBuffer.h` | ✅ Phase 3c 完成 2026-02-23 |
| `VKBufferWriteAgent.h` | 单实现抽象接口（dead abstraction 已文档说明） | ✅ DEAD-3 已处理 |
| `VKRingBufferWrapper.h/.cpp` | BufferWriteAgent 唯一实现，路径已文档化 | ✅ 清晰 |
| `DeviceBufferRingWriter.h` | Ring buffer 帧偏移计算器 | ✅ 职责单一 |
| `VKMemoryAllocator.h/.cpp` | 旧式 UBO/SSBO 分配器（VulkanArrayBuffer 使用），豁免迁移 | ⚠️ 孤立使用，见注意事项 1 |
| `VKArrayBuffer.h/.cpp` | VulkanArrayBuffer，使用旧式 Map/Flush，豁免迁移 | ⚠️ 孤立使用，见注意事项 1 |
| `RenderBufferUploadSystem.cpp` | ECS 上传调度 | ✅ 清晰 |

---

## 五、注意事项

1. `VulkanArrayBuffer` + `VKMemoryAllocator` 是旧式路径，未接入 `IGPUBuffer` registry，其 `Map/Flush` 直接操作 CPU-visible memory，与 ECS 上传系统无关。在没有将其迁移到 `IGPUBuffer` 之前，不要修改其调用方式。

2. `ResolvePolicy` 在 `VKDeviceBuffer.cpp` 中会将 `Auto` 解析为 `CPUVisible`（有 ReBAR）或 `StagedUpload`（无 ReBAR）。在 ReBAR 设备上，所有 `StagedBuffer` 路径会被跳过，直接创建 CPU-visible buffer——此时 `IsDirty()` 永远不会为 true，`RenderBufferUploadSystem` 对其无操作，这是正确行为。

3. 在修改 `StagedBuffer` 之前，确认 `VkMemoryPropertyFlags` 中 `HOST_COHERENT` 的存在：有 `HOST_COHERENT` 时 CPU 写入自动可见，`Flush` 调用可以是空操作；没有时必须显式 `vkFlushMappedMemoryRanges`。当前 `StagedBuffer` 的 staging memory 总是 `HOST_VISIBLE | HOST_COHERENT`（见 `VKDeviceStagedBuffer.cpp`），所以精确 dirty range 主要影响的是 `vkCmdCopyBuffer` 的 srcOffset/size，而不是 host flush。
